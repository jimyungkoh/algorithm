package topics.graph;

/**
 * 이분 매칭 템플릿.
 *
 * 목표:
 * - 좌측 정점 기준 최대 매칭을 계산한다.
 * - 반환 배열의 각 인덱스는 좌측 정점의 매칭 상대를 의미한다.
 *
 * 계약:
 * - graph: 좌측 정점 i가 연결 가능한 우측 정점 목록을 담은 인접 배열
 * - rightCount: 우측 정점 개수
 * - 반환: 좌측 정점별 매칭 결과 배열 (-1은 미매칭)
 */
public class Matching {
    public int[] maximumBipartiteMatching(int[][] graph, int rightCount) {
        throw new UnsupportedOperationException("TODO: implement");
    }
}
