package topics.graph;

/**
 * 프림 MST 템플릿.
 *
 * 목표:
 * - 무방향 가중치 그래프에서 시작 정점 기반 최소 신장 트리를 구성한다.
 * - 정점이 n개일 때 n-1개의 간선을 반환한다.
 *
 * 계약:
 * - vertexCount: 정점 개수
 * - edges: [from,to,weight] 형태의 간선 목록
 * - start: 시작 정점
 * - 반환: MST를 구성하는 간선 배열
 * - 연결되지 않은 그래프는 빈 배열을 반환한다.
 */
public class Prim {
    public int[][] prim(int vertexCount, int[][] edges, int start) {
        throw new UnsupportedOperationException("TODO: implement");
    }
}
